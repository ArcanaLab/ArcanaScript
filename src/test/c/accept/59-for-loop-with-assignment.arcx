class ForLoopWithAssignmentTest {
    # Test for for loop with assignment inside
    #sum:int = 0;
    #numbers:List<int> = [1, 2, 3, 4, 5];
    
    result : Function<void> = () {
        sum: int = 0;
        for(num : numbers) {
            sum = sum + num;
        }
    };
}
