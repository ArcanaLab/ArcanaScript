class ForLoopNestedTest {
    # Test for nested for loops
    rows:int[] = [1, 2, 3];
    cols:int[] = [4, 5, 6];
    
    for(row : rows) {
        for(col : cols) {
            print(row + col);
        }
    } 
}
