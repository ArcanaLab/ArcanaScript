class ForLoopWithReturnTest {
    # Test for for loop with return
    # numbers:List<int> = [1, 2, 3, 4, 5];
    for(num : numbers) {
        if (num == 3) {
          #  return num;
          result: int = num;
        }
    } 
}
