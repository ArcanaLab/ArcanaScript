class LesserTest {
    lesserTestFunction: Function<Integer> = (x:int) {
        vara:int = 0;
        if (x<=6)
        {
            vara=1;
        }
        if(x > 6)
        {
            vara =2;
        }
        return vara;
    };
}
