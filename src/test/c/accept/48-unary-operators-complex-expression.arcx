class UnaryOperatorsComplexExpressionTest {
    result : Function<Boolean> = () {
        a: int = 5;
        b: int = 3;
        c: int = 2;
        result: int = (a++ * b--) + (c++ - a--);
        return true;
    };
}
