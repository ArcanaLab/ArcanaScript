class ForLoopWithFunctionCallTest {
    # Test for for loop with function call
    # names:List<string> = ["Alice", "Bob", "Charlie", "Diana"];
    
    result : Function<void> = () {
        names: List<string> = ["Alice", "Bob", "Charlie", "Diana"];
        for(name : names) {
            greet(name);
        }
    };
}
