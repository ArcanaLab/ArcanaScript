class GreaterEqualOperatorTest {
    # Test for >= (greater than or equal) operator
    GreaterEqualOperator : Function<void> = () {
        vara:int = 8;
        varb:int = 8;
        if (vara >= varb)
        {
            result:boolean = true;
        }
        return;
    };
}
