class GreaterThanOperatorTest {
    # Test for > (greater than) operator
    GreaterThanOperator : Function<void> = () {
        vara:int = 10;
        varb:int = 3;
        if (vara > varb)
        {
            result:boolean = true;
        }
        return;
    };
}
