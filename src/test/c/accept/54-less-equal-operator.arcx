class LessEqualOperatorTest {

    # Test for <= (less than or equal) operator
    Function : Function<void> = () {
        vara:int = 5;
        varb:int = 5;
        
        if (vara <= varb)
        {
            result:boolean = true;
        }
        return;
    };

}
