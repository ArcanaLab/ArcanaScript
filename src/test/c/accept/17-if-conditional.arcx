class IfConditionalTest {

    FunctionTest : Function<Integer> = (a:int) {
        if (a >= 10){
            return a + 10;
        } else if (a >= 5) {
            return a + 5;
        } else {
            return a;
        }
    };
}
