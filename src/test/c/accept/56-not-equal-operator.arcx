class NotEqualOperatorTest {
    # Test for != (not equal) operator
    NotEqualOperator : Function<void> = () {
        vara:int = 5;
        varb:int = 10;
        if (vara != varb)
        {
            result:boolean = true;
        }
        return;
    };
}
