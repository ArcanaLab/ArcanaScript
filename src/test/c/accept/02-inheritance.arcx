class ShireClass is FatherClass
{
		constructor(hasSecondBreakfast:bool, hungerMeter:float)
		{
			if(hasSecondBreakfast == false)
			{
				print("What about second breakfast");
			}
			super(hungerMeter);
		}
}