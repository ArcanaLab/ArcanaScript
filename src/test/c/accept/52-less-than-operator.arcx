class LessThanOperatorTest {
    # Test for < (less than) operator
    LessThanOperator : Function<void> = () {
        vara:int = 3;
        varb:int = 7;
        if (vara < varb)
        {
            result:boolean = true;
        }
        return;
    };
}
