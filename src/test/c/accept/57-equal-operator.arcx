class EqualOperatorTest {
    # Test for == (equal) operator
    EqualOperator : Function<void> = () {
        vara:int = 5;
        varb:int = 5;
        if (vara == varb)
        {
            result:boolean = true;
        }
        return;
    };
}
