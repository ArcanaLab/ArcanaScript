class FunctionCallTest {
    funA = () {};
    funA();
    
    funB = (a: int, b: int) {};
    funB(1, 2);
    
    funC: Function<Integer> = (a: int, b: int) { 
        while(true) {
            return;
        }
    };
    funC(funA(), funB(1, 2));
    
}
